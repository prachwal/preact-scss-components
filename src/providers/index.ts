/**
 * Providers module exports
 *
 * This module contains context providers and theme management utilities
 * for the Preact SCSS Components library.
 */

// Main theme provider component and context
export { ThemeProvider, ThemeContext } from './ThemeProvider';

// Theme utility functions for labels and icons
export { getThemeLabel, getThemeIcon } from './ThemeProvider';